@model List<ServerModel>
@{
    ViewData["Title"] = "Servers";
}

<h1>Servers</h1>
<hr>
@foreach (var server in Model)
{
    <table width="600" cellspacing="0">
        <tbody>
            <tr>
                <td bgcolor="#cccccc">@server.Title > <a href="/Server/Index/@server.Id">📎</a></td>
            </tr>
            <tr>
                <td bgcolor="#000000">
                    <font color="cccccc"> 
                        RAM  ~ [ 🇬🇧: <font color="lightgreen">@server.Memory</font> ]
                    </font>
                </td>
            </tr>
        </tbody>
    </table>
    <hr>
}